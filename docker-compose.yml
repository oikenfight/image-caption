version: '3'

services:
  workspace_oikawa:
    image: workspace_oikawa
    build:
      context: ./
      dockerfile: docker/workspace/Dockerfile
    ports:
      - "5000:5000"
      - "8080:8080"
      - "8888:8888"
    volumes:
      - nvidia_driver_375.66:/usr/local/nbidia:ro
      - ./:/workspace
    working_dir: "/workspace"
    tty: true
  python:
    depends_on:
      - workspace_oikawa
    build:
      context: ./
      dockerfile: ./docker/
    volumes:
      - nvidia_driver_375.66:/usr/local/nbidia:ro
      - ./:/workspace
    working_dir: "/workspace"
volumes:
  nvidia_driver_375.66:
      external: true